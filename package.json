{
  "name": "gtasks-notion-sync",
  "version": "1.0.0",
  "description": "Synchronise Google Tasks vers Notion automatiquement",
  "main": "api/cron-sync.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build complete'",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "googleapis": "^118.0.0",
    "@notionhq/client": "^2.2.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {}
}
